# UVBF_M2_Crypto

```sh
pip install pipenv # Install Pipenv
pipenv install # Install packages declared in `Pipefile`
pipenv shell # Activate the Virtual Env
jupert-lab --port <port> # Run jupyterlab
```
